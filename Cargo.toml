[package]
name = "wist-delivery"
version = "0.1.1" #@gxl:set(version)
edition = "2024"
license = "Apache-2.0"
rust-version = "1.85"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
